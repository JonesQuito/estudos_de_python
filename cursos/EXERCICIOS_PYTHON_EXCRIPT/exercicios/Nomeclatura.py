"""
    Variáveis e métodos/funções devem ser nomeados com letras minusculas.
    Caso haja nomes compostos devem ser separados com o caracter( _ ) e não usar letras maiúsculas.
    EX: enviar_email()

    Nomes de classe devem se escritos com a inicial maiúscula e,
    caso seja nome composto, as palavras subsequêntes devem ser iniciadas, também com letra maiúscula.
    EX: class PessoaFisica

    Constantes dever ser nomeadas com letras em caixa alta e caso seja um nome composto
    por duas palavras ou mais, separá-las com um underscore(_)
    EX: PI
        PARAMETRO_INICIAL

    Parâmetros de função, assim como nomes de variáveis dever ser escritos com letras minusculas
    e caso seja um nome composto, as palavras que a compoem devem ser separadas por underscore( _ )
    EX: enviar(nome_do_arquivo)
        receber(parametro_de_entrada)
"""